// Package http in trasport layer is to
// accept request -> decode Request -> call the endpoint.function -> recieve response or error and encode accordingly.
// ErrorResponse Should always be included in any error responses
package http
